# Routing — 2026-07-28

## Rozhodnutí: FALLBACK-AUDIT → `clanek-nadeje-doziti-vzdelani.html`

## Dní od posledního nového článku: 1
`clanek-zijici-darci-ledvin` (2026-07-27). Kadenční pojistka (>2 dny bez nového článku) NENÍ aktivní; týdenní kvóta ≥3 nových článků splněna. Publikační fronta saturovaná (32× `published:false`).

## Důvod (3–5 vět)
1. Discovery nezachytilo HOT trigger dost silný na NOVÝ článek ani WARM (žádná nová vlna dat nezastaralila konkrétní článek); jediná nová norma (novela zák. o ochraně veřejného zdraví, pitná voda, 13. 7. 2026) je úzce technická.
2. Zadání session klade **absolutní důraz na validaci a ověření všech zdrojů** → nejhodnotnější akce je nezávislý audit publikovaného článku proti primárním zdrojům.
3. Cíl vybrán jako **publikovaný** článek se statusem `review-pending`, který **nikdy neprošel nezávislým fact-auditem** (v HTML žádný `f*_` audit-záznam, `last_reviewed` jen z tvorby). Auditem se buď potvrdí (→ verified), nebo najde defekt.

## Výběr cíle
- `clanek-nadeje-doziti-vzdelani` — publikovaný (live čtenářům), `audit-status: review-pending`, 7 registrovaných tvrzení v `claims.json`, jádro = headline KPI „13 let". Bohatý na kvantitativní, mezinárodně ověřitelná tvrzení (Eurostat, ČSÚ, Katrňák & Hamplová).

## Ověření (primární zdroje) — výsledek: **PASS**
- **Jádro 13 let** (muži, věk 25, terciární vs. základní): ověřeno **verbatim** v H7O (Katrňák & Hamplová, CDK 2018): „Rozdíl v naději dožití ve věku 25 let … je u nás pro muže asi 13 let." ✓
- **2. místo z 23 zemí, jen Maďarsko výš (~14 let)**: verbatim H7O: „Z 23 evropských zemí má tento rozdíl vyšší jen Maďarsko (asi 14 let)." ✓
- **Ženy ~5 let**: verbatim H7O: „U žen je ten samý rozdíl … necelých 5 let." ✓
- **Eurostat demo_mlexpecedu (muži, věk 30, 2017)** — přes Eurostat API, gap terciární−základní **přesně sedí na desetinu**:
  - Slovensko 14,8 ✓ · Polsko 11,0 ✓ · Maďarsko 10,7 ✓ · Slovinsko 5,7 ✓
- **Zdrojové odkazy (HTTP + obsah):** csu.gov.cz/obyvatelstvo (live, odkaz „Zemřelí, naděje dožití, příčiny smrti") ✓; soc.cas.cz aktualita (správná URL, existuje) ✓; ec.europa.eu/eurostat demo_mlexpecedu (validní dataset) ✓; h7o.cz (200, obsah potvrzuje) ✓.
- **Datový kontrakt:** indikátor `nadeje_doziti_vzdelani_gap_muzi` (value 13, signal bad) v `data/indicators.json`, metodická karta existuje, databox odkazy `indikator-*.html` platné. ✓

## Poznámka k nuanci (bez opravy textu)
- Tisková zpráva Sociologického ústavu (soc.cas.cz, Hamplová) uvádí rozdíl **11 let** — jiná věková/rounding reference než jádro článku (věk 25, 23-zemní studie = 13 let, potvrzeno H7O). Článek headline korektně atribuuje 13 let ke Katrňák & Hamplová a věk 25 explicitně uvádí; odkaz na soc.cas.cz je veden jako obecný český zdroj o nerovnostech, nikoli jako zdroj čísla 13. → Bez zásahu do textu; citlivost na věkovou referenci článek už explicitně vysvětluje (u Eurostat tabulky věk 30).

## Akce
Audit-pass: povýšit `review-pending → verified`, doplnit audit-záznam do HTML (last_reviewed 2026-07-28 + zdrojová verifikace), synchronizovat `data/articles.json` a JSON-LD `dateModified`. Bez změny věcného obsahu — všechna tvrzení ověřena proti primárním zdrojům.
