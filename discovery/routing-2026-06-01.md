# Routing report — 2026-06-01

## Rozhodnutí: FALLBACK-AUDIT (priorita „riziko nepřesnosti")

Cíl: **`clanek-rezistence-antibiotik.html`** (publikovaný 31. 5. 2026).
Discovery 1. 6. nepřinesl nový primárně-zdrojový trigger (vše z 26.–30. 5.
zrouteováno). Při validaci (`npm run validate:all`) odhaleny dva problémy
v tomto publikovaném článku — jeden hygienický (opraveno), jeden zásadní
faktický (flagged).

## Důvod (3–5 vět)

Uživatel explicitně zdůraznil „naprosto zásadní validace a ověření všech
zdrojů". `validate:all` na startu hlásil 3 blocking errors (reader-viditelný
redakční banner v publikovaném článku rezistence-antibiotik) — odstraněno.
Při auditu centrálních čísel téhož článku jsem proti primárnímu zdroji
(ECDC EARS-Net AER 2024, plný PDF) ověřil, že **benchmark „OECD 11,5 %"
je metodicky chybný a hodnotově ~2× nižší** než skutečný EARS-Net EU/EEA
populačně vážený průměr **22,5 %** (E. coli, fluorochinolony, 2024).
Tím padá thesis „Česko zaostává oproti průměru"; navíc česká hodnota
13,1 % má `origin: seed` a nebyla primárně ověřena. Per železné pravidlo
(„co není ověřené, na portálu nezůstává") a Phase 5 (zásadní problém)
článek flaguju a stahuju z produkce do ověření redakcí.

## Provedené změny (5 fází → commit per fáze)

### Phase 1 — Discovery
- `discovery/discovery-2026-06-01.md` (no-new-trigger + nález A/B).

### Phase 2 — Routing
- `discovery/routing-2026-06-01.md` (tento soubor).

### Phase 3 — Creation/Revision (audit-fix + audit-flag)
- **`clanek-rezistence-antibiotik.html`**:
  - odstraněn reader-viditelný redakční banner (`<p style="…#fff7e6…">`)
    — hygiena (nález A);
  - HTML meta `article:audit-status` → `flagged`;
  - opraveny provably-false výskyty „OECD 11,5 %" v perexu, meta
    description, og:description, databoxu → primárně ověřená formulace
    „EU/EEA EARS-Net průměr 22,5 % (2024)";
  - přidán prominentní `article-callout-caveat` o flagged stavu
    a o tom, že česká hodnota 13,1 % čeká na primární EARS-Net verifikaci;
  - aktualizován audit YAML komentář (last_reviewed 2026-06-01, finding B).
- **`data/articles.json`** → `rezistence-antibiotik`: `published: false`,
  `audit-status: "flagged"` (vynuceno validátorem: flagged ⟹ published:false).
- **`data/indicators.json`** → `rezistence_antibiotik_ecoli`:
  - `benchmark`: odstraněno chybné `oecd: 11.5`, nastaveno `eu: 22.5`
    (ECDC EARS-Net AER 2024, EU/EEA populačně vážený průměr);
  - `signal`: `warn` → `neutral` (česká hodnota neověřena → nelze
    klasifikovat); `direction` zachováno `lower_is_better`;
  - `source`: name → "ECDC EARS-Net (AER 2024)", url → AER 2024 PDF,
    `fetched_at` 2026-06-01, `origin` zůstává `seed` (česká hodnota
    nadále neověřena — flag).
- **`indicators/rezistence_antibiotik_ecoli.json`** (metodická karta):
  patient_story — opraveno „OECD průměr 11,5 %, EU 12,0 %" → „EU/EEA
  EARS-Net průměr 22,5 % (2024)"; doplněn `benchmark` + caveat o seed
  hodnotě.

### Phase 4 — Enhancement
- Žádné nové AV počítadlo s neověřeným číslem. Caveat callout = jediný
  nový vizuální prvek (text-only, primárně ověřený).

### Phase 5 — Independent audit + GitHub issue
- GitHub issue (mcp__github__issue_write) s detailem nálezu B
  a otevřenou otázkou primární verifikace české hodnoty.

## Co k publikaci vyžaduje ruční schválení redakce
- `clanek-rezistence-antibiotik.html` — **flagged**, staženo (published:false).
  Redakce musí (1) ověřit českou hodnotu E. coli FQ rezistence proti
  ECDC Surveillance Atlas / SZÚ EARS-Net, (2) rozhodnout o re-thesis
  (po opravě benchmarku 22,5 % se mění vyznění), (3) napojit indikátor
  na živý EARS-Net výstup (origin seed → live).
</content>
